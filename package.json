{
  "name": "gnss-proxy-server",
  "version": "1.0.0",
  "description": "Serveur proxy sécurisé pour l'API OpenWeatherMap.",
  "main": "server.js",
  "scripts": {
    "start": "OPENWEATHER_API_KEY=$OPENWEATHER_API_KEY node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^2.6.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
